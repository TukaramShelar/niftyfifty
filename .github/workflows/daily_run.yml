name: Daily Market Data Fetcher

on:
  schedule:
    # 8:55 AM IST (3:25 AM UTC) - Early Morning Pre-Market Run
    - cron: '25 3 * * 1-5'
    # 6:45 PM IST (1:15 PM UTC) - Post-Market Final Data Logging Run
    - cron: '15 13 * * 1-5'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests gspread google-auth yfinance

    - name: Execute Data Fetch Script
      env:
        GCP_SERVICE_ACCOUNT: ${{ secrets.GCP_SERVICE_ACCOUNT }}
        SPREADSHEET_NAME: "NiftyDailyData"
      run: python main.py
